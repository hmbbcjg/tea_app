{
  "name": "tea-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "茶记 - 茶叶消耗记录 App",
  "scripts": {
    "sync": "cap sync android",
    "android:open": "cap open android",
    "apk": "cd android && gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "@capacitor/android": "^6.2.0"
  }
}
